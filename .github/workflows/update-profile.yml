name: Update profile

on:
  schedule:
    - cron: '23 6 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - profile.json
      - 'scripts/*.py'
      - '**/avatar.txt'
      - assets/avatar-tones.json
      - .github/workflows/update-profile.yml

permissions:
  contents: write

concurrency:
  group: profile-${{ github.ref }}
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Refresh public profile data
        env:
          PROFILE_BIRTH_DATE: ${{ secrets.PROFILE_BIRTH_DATE }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/update_profile.py --refresh

      - name: Commit generated files
        id: commit
        shell: bash
        run: |
          initial_commit="$(git rev-parse HEAD)"
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- assets/neofetch.svg assets/neofetch-mobile.svg assets/neofetch-still.svg assets/neofetch-mobile-still.svg assets/stats.json
          if ! git diff --cached --quiet; then
            git commit -m 'Update public profile assets'
            python scripts/update_profile.py --pin-images-to "$(git rev-parse HEAD)"
          elif grep -q '/main/assets/neofetch' README.md; then
            python scripts/update_profile.py --pin-images-to "$(git rev-parse HEAD)"
          fi
          git add -- README.md
          if ! git diff --cached --quiet; then
            git commit -m 'Point profile to current immutable assets'
          fi
          if [ "$(git rev-parse HEAD)" = "$initial_commit" ]; then
            echo 'changed=false' >> "$GITHUB_OUTPUT"
          else
            echo 'changed=true' >> "$GITHUB_OUTPUT"
          fi

      - name: Push generated files
        if: steps.commit.outputs.changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          git -c credential.helper= \
            -c 'credential.helper=!f() { printf "%s\n" "username=x-access-token" "password=$GITHUB_TOKEN"; }; f' \
            push origin "HEAD:${GITHUB_REF_NAME}"
